@model CrossChat.Data.Entities.User

<!DOCTYPE html>
<html lang="ru">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Личный кабинет</title>
    <link href="https://fonts.googleapis.com/css2?family=Inter:wght@400;600&display=swap" rel="stylesheet">
    <style>
        :root {
            --bg-color: #050505;
            --glass-bg: rgba(255, 255, 255, 0.05);
            --glass-border: rgba(255, 255, 255, 0.1);
            --text-main: #ffffff;
            --text-secondary: #a1a1aa;
        }

        body {
            font-family: 'Inter', sans-serif;
            background-color: var(--bg-color);
            color: var(--text-main);
            margin: 0;
            padding: 0;
            overflow-x: hidden;
        }

        /* NAVBAR - Логотип слева */
        .navbar {
            display: flex;
            justify-content: space-between;
            align-items: center;
            padding: 1rem 1.5rem;
            background: rgba(5, 5, 5, 0.9);
            border-bottom: 1px solid var(--glass-border);
        }

        .logo-container {
            display: flex;
            align-items: center;
            gap: 10px;
            text-decoration: none;
        }

        .logo-img {
            width: 32px;
            height: 32px;
            border-radius: 8px;
            object-fit: cover;
        }

        .logo-text {
            font-weight: 700;
            color: white;
            font-size: 1.1rem;
        }

        .user-menu {
            display: flex;
            align-items: center;
            gap: 1rem;
        }

        .user-email {
            color: var(--text-secondary);
            font-size: 0.9rem;
        }

        @@media(max - width: 600px)

        {
            .user-email

        {
            display: none;
        }

        }

        .logout-btn {
            background: transparent;
            border: 1px solid var(--glass-border);
            color: white;
            padding: 6px 12px;
            border-radius: 8px;
            cursor: pointer;
        }

        .container {
            max-width: 600px;
            margin: 2rem auto;
            padding: 0 1rem;
        }

        .card {
            background: var(--glass-bg);
            border: 1px solid var(--glass-border);
            border-radius: 16px;
            padding: 1.5rem;
            margin-bottom: 1.5rem;
        }

        /* КНОПКА INSTAGRAM (Фикс) */
        .btn-instagram {
            display: flex;
            align-items: center;
            justify-content: center;
            gap: 8px;
            width: 100%; /* На всю ширину */
            box-sizing: border-box; /* Учитываем padding */
            padding: 12px;
            background: #262626;
            border: 1px solid #404040;
            border-radius: 8px;
            color: white;
            text-decoration: none;
            font-weight: 600;
            margin-top: 1rem;
            transition: background 0.2s;
        }

            .btn-instagram:hover {
                background: #333;
            }

        /* СТАТУСЫ */
        .status-badge {
            display: inline-flex;
            align-items: center;
            gap: 6px;
            padding: 4px 10px;
            border-radius: 20px;
            font-size: 0.75rem;
            font-weight: 600;
            text-transform: uppercase;
        }

        .status-disconnected {
            background: rgba(255,255,255,0.1);
            color: #888;
        }

        .status-active {
            background: rgba(16, 185, 129, 0.2);
            color: #10b981;
            border: 1px solid rgba(16, 185, 129, 0.3);
        }

        .status-paused {
            background: rgba(234, 179, 8, 0.2);
            color: #fbbf24;
            border: 1px solid rgba(234, 179, 8, 0.3);
        }

        .dot {
            width: 6px;
            height: 6px;
            border-radius: 50%;
            background-color: currentColor;
        }

        .status-active .dot {
            animation: pulse 2s infinite;
        }
        @@keyframes pulse {
            0%

        {
            opacity: 1;
        }

        50% {
            opacity: 0.4;
        }

        100% {
            opacity: 1;
        }

        }
    </style>
</head>
<body>

    <nav class="navbar">
        <a href="/" class="logo-container">
            <img src="/images/CrossChatLogo.jpeg" alt="Logo" class="logo-img">
            <span class="logo-text">CrossChat</span>
        </a>
        <div class="user-menu">
            <span class="user-email">@Model.Email</span>
            <form action="/auth/logout" method="post" style="margin:0">
                <button class="logout-btn">Выйти</button>
            </form>
        </div>
    </nav>

    <div class="container">
        <!-- Приветствие -->
        <div class="card" style="display:flex; align-items:center; gap:15px;">
            @if (!string.IsNullOrEmpty(Model.AvatarUrl))
            {
                <img src="@Model.AvatarUrl" style="width:50px; height:50px; border-radius:50%;" alt="Avatar">
            }
            else
            {
                <div style="width:50px; height:50px; border-radius:50%; background:#333; display:flex; align-items:center; justify-content:center;">
                    @Model.Name.Substring(0, 1)
                </div>
            }
            <div>
                <h2 style="margin:0; font-size:1.2rem;">Привет, @Model.Name!</h2>
                <span style="color:var(--text-secondary); font-size:0.9rem;">ID: #@Model.Id</span>
            </div>
        </div>

        <!-- Карточка Instagram -->
        <div class="card">
            <div style="display:flex; justify-content:space-between; align-items:center; margin-bottom:1rem;">
                <h3 style="margin:0; font-size:1.2rem;">Instagram Бот</h3>
                @if (Model.InstagramSettings != null && !string.IsNullOrEmpty(Model.InstagramSettings.AccessToken))
                {
                    if (Model.InstagramSettings.IsActive)
                    {
                        <span class="status-badge status-active"><span class="dot"></span>Активен</span>
                    }
                    else
                    {

                        <span class="status-badge status-paused"><span class="dot"></span>На паузе</span>
                    }
                }
                else
                {

                    <span class="status-badge status-disconnected">Не подключено</span>
                }
            </div>

            @if (Model.InstagramSettings != null && !string.IsNullOrEmpty(Model.InstagramSettings.AccessToken))
            {
                <div style="display: flex; align-items: center; gap: 12px; margin-bottom:1rem;">
                    @if (!string.IsNullOrEmpty(Model.InstagramSettings.ProfilePictureUrl))
                    {
                        <img src="@Model.InstagramSettings.ProfilePictureUrl" style="width: 48px; height: 48px; border-radius: 50%; border:2px solid #333;">
                    }
                    <div>
                        <div style="font-weight:600;">@Model.InstagramSettings.Username</div>
                        <div style="font-size:0.85rem; margin-top:2px;">
                            @if (Model.InstagramSettings.IsActive)
                            {
                                <span style="color:#10b981;">● Бот отвечает на сообщения</span>
                            }
                            else
                            {

                                <span style="color:#fbbf24;">⚠️ Бот выключен</span>
                            }
                        </div>
                    </div>
                </div>

                <a href="/instagram" class="btn-instagram">
                    ⚙️ Настроить и @(Model.InstagramSettings.IsActive ? "Выключить" : "Включить")
                </a>
            }
            else
            {
                <p style="color:#aaa; font-size:0.9rem; margin-bottom:0;">Подключите аккаунт, чтобы настроить автоответы.</p>
                <a href="/instagram" class="btn-instagram">
                    + Подключить Instagram
                </a>
            }
        </div>
    </div>

</body>
</html>